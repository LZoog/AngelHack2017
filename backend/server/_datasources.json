{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongo": {
    "host": "${MONGO_HOST:localhost}",
    "port": ${MONGO_PORT:27017},
    "url": "",
    "database": "${MONGO_DATABASE:loopback}",
    "password": "${MONGO_PASSWORD}",
    "name": "mongo",
    "user": "${MONGO_USER}",
    "connector": "mongodb"
  }
}
